function copyText(id: string) {
  // const copyEl = document.getElementById(id);
  // if(copyEl){
  //   copyEl.onselect();
  //   copyEl.setSelectionRange(0, 99999);   // 모바일 브라우저(ios)에서의 동작을 위한 추가코드
  //   document.execCommand('copy');
  //   copyEl.setSelectionRange(0, 0);       // 모바일 브라우저(ios)에서의 동작을 위한 추가코드
  // } 나중에 ref 사용하게 만들어보자..
}

export default copyText