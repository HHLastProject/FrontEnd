import styled from 'styled-components'

//아무 스타일도 없는 버튼
const BtnResetStyle = styled.button`
  background: inherit; 
  border: none; 
  box-shadow: none; 
  border-radius: 0; 
  padding: 0;
  overflow: visible;
  font-size: inherit;
  font-weight: inherit;
  cursor: pointer;
`;

export default BtnResetStyle